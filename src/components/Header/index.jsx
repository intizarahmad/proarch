import React from 'react'

import './index.css'
/**
 * Even we just have a grey box as header, still I am creating for future use 
 * @returns <header />
 */
const index = () => <header className="header" />

export default index
